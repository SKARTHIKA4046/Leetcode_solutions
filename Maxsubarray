class Solution:
    def maxSubArray(self, nums: List[int]) -> int:
        list=[]
        sum=0
        for i in range(len(nums)):
            sum =sum+nums[i]
            list.append(sum )
            if sum<0:
                sum=0
        return max(list)
